/target
.claude/
*.pdf

# Gas Town (added by gt)
.runtime/
.logs/

# Dolt database files (added by bd init)
.dolt/
*.db
*.pid.lock

# Beads backup/sync state (synced via Dolt remote)
.beads/backup/
.beads/push-state.json

# Fuzz artifacts (crashes, corpus, coverage)
fuzz/artifacts/
fuzz/corpus/
fuzz/coverage/
fuzz/target/
